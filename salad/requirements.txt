# Qwen QLoRA fine-tuning dependencies (containerized)
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.42.0
accelerate>=0.28.0
tqdm

# Cloud upload (install what you need)
google-cloud-storage
boto3
